# 日常クエストRPG separated files

このフォルダは、元の1枚HTMLを Codex に渡しやすいように分離したものです。

## ファイル構成

- `index.html`：HTML構造だけ
- `styles.css`：見た目・レスポンシブ・装飾
- `app.js`：LocalStorage、タスク追加、達成処理、報酬購入、描画処理

## 起動方法

ビルド不要です。`index.html` をブラウザで開くだけで動きます。

## 注意

データはブラウザの `LocalStorage` に保存されます。サーバー通信・ログイン機能はありません。
