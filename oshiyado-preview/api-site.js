const zlib = require('zlib');
const PARTS = [1,2,3,4,5,6].map(n => `https://raw.githubusercontent.com/lolbeginnercourse/mainitiworakunisuru/oshiyado-production/oshiyado-preview/bundle.part${String(n).padStart(2,'0')}`);
let cache;
async function loadBundle(){if(cache)return cache;const chunks=await Promise.all(PARTS.map(async url=>{const r=await fetch(url,{headers:{'User-Agent':'oshiyado-preview'}});if(!r.ok)throw new Error(`bundle fetch failed: ${r.status} ${url}`);return r.text();}));cache=JSON.parse(zlib.brotliDecompressSync(Buffer.from(chunks.join(''),'base64')).toString('utf8'));return cache;}
function contentType(name){if(name.endsWith('.html'))return'text/html; charset=utf-8';if(name.endsWith('.json'))return'application/json; charset=utf-8';if(name.endsWith('.txt'))return'text/plain; charset=utf-8';if(name.endsWith('.xml'))return'application/xml; charset=utf-8';return'application/octet-stream';}
module.exports=async(req,res)=>{try{const files=await loadBundle();let p=String((req.query&&req.query.path)||'').replace(/^\/+/, '');try{p=decodeURIComponent(p)}catch(_){}if(!p)p='index.html';if(p.endsWith('/'))p+='index.html';if(!files[p]&&!p.includes('.')&&files[p+'/index.html'])p+='/index.html';const found=Object.prototype.hasOwnProperty.call(files,p);const body=found?files[p]:(files['404.html']||'Not Found');res.statusCode=found?200:404;res.setHeader('Content-Type',contentType(found?p:'404.html'));res.setHeader('X-Robots-Tag','noindex, nofollow');res.setHeader('X-Content-Type-Options','nosniff');res.setHeader('Referrer-Policy','strict-origin-when-cross-origin');res.setHeader('Permissions-Policy','camera=(), microphone=(), geolocation=(), payment=()');res.setHeader('X-Frame-Options','SAMEORIGIN');res.setHeader('Cache-Control','public, max-age=0, must-revalidate');res.end(body);}catch(err){console.error(err);res.statusCode=500;res.setHeader('Content-Type','text/plain; charset=utf-8');res.setHeader('X-Robots-Tag','noindex, nofollow');res.end('Preview load error');}};
